// GOAL: Provide a minimal ESLint flat configuration for the repository.\n// RESPONSIBILITY: Keep generated build output out of linting while allowing the TypeScript project to lint.\n// DOES NOT: Add framework-specific lint rules before the codebase needs them.\nexport default [\n  // Ignore generated output because it is not source code maintained by the developer.\n  { ignores: ["dist/**", ".output/**", "node_modules/**"] },\n];\n